@using WebApp.Models
@model List<FrBirimler>
@{

    Layout = null;
}

<div class="panel-body" style="padding: 0px;">
    <div class="panel panel-primary">
        <div class="panel-body padding-0">
            <table class="table table-hover" style="width:100%;table-layout:fixed;margin-bottom:0px;">
                <thead>
                    <tr class="warning">
                        <th width="80">B Kod</th>
                        <th>Birim Adı</th>
                        <th style="text-align:right;" width="100">Veri Giriş Durumu</th>
                        <th style="text-align:right;" width="100">Veri Onay Durumu</th>
                    </tr>
                </thead>
                @foreach (var itemObt in Model)
                {
                    <tr class="tr">
                        <td>@itemObt.BirimKod</td>
                        <td>
                            @itemObt.BirimAdi
                        </td>
                        <td width="100" style="text-align:right;">
                            @if (itemObt.ToplamCount == itemObt.TamamlananCount)
                            {
                                <span style="color: green;">Veri Girişi Tamamlandı</span>
                            }
                            else
                            {
                                <span style="color: maroon;">Veri Girişi Tamamlanmadı</span>
                            }
                        </td>
                        <td width="100" style="text-align:right;">
                            @if (itemObt.ToplamCount == itemObt.OnaylananCount)
                            {
                                <span style="color: green">Veriler Onayı Tamamlandı</span>
                            }
                            else
                            {
                                <span style="color: maroon;">Veri Onayı Tamamlanmadı</span>
                            }
                        </td>
                    </tr>

                }
            </table>
        </div>
    </div>
</div>